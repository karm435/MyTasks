import Foundation
import SwiftUI

struct PersonView: View {
	var body: some View {
		VStack {
			//TODO: Update the view with real code
			Text("Sample code")
		}
		.frame(width: 100) //TODO: Remove fixed width
	}
}

#Preview {
	PersonView()
}
